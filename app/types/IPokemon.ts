/**
 * Represents a single pokemon as model.
 */
export interface IPokemon {
    /** The unique identifier in the pokedex. */
    id: number;
    /** The name for the requested language. */
    name: string;
}

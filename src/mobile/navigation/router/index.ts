import { gameNavKey, gameSettingsNavKey, settingsNavKey, settingsStackNavKey } from './constants';
import Router from './routerComponent';

export { Router as default, gameNavKey, gameSettingsNavKey, settingsNavKey, settingsStackNavKey };

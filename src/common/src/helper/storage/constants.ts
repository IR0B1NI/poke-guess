/** The name of the key in the storage to store the current game progress. */
const saveStoreKey = 'PokemonGameSave';
/** The name of the key in the storage to store the cached poke api data.*/
const pokeApiDataCacheKey = 'PokeApiData';

export { pokeApiDataCacheKey, saveStoreKey };
